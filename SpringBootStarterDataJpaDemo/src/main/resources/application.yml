spring:
  datasource:
    # 数据库连接地址
    url: jdbc:postgresql://172.16.18.240:5432/pg
    # 数据库驱动
    driver-class-name: org.postgresql.Driver
    # 数据库用户
    username: pg
    password: 123456
  jpa:
    hibernate:
      # 更新或者创建数据表
      ddl-auto: update
    # 控制台显示sql
    show-sql: true
